<?xml version="1.0" encoding="utf-8"?>
<!-- Post-connect addressbook selection: the connected account's
     addressbooks, each with a checkbox; Continue subscribes the
     checked ones and runs the first sync. Styled like the config
     panel (app bar, title, paragraph, list, fixed bottom Continue). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="24dp"
        android:paddingTop="24dp"
        android:paddingEnd="24dp"
        android:text="@string/books_title"
        android:textAppearance="@android:style/TextAppearance.DeviceDefault.Widget.ActionBar.Title"
        android:textColor="?android:attr/textColorPrimary" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:text="@string/books_description"
        android:textColor="?android:attr/textColorSecondary"
        android:textSize="14sp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:id="@+id/books_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingEnd="24dp" />

    </ScrollView>

    <!-- Disabled while nothing is checked; while syncing the loader
         sits on the button (disabled again, its label blanked). -->
    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_margin="@dimen/fab_margin">

        <ImageButton
            android:id="@+id/books_continue"
            android:layout_width="@dimen/fab"
            android:layout_height="@dimen/fab"
            android:background="@drawable/fab_background"
            android:contentDescription="@string/email_submit"
            android:elevation="@dimen/fab_elevation"
            android:src="@drawable/ic_arrow_forward" />

        <ProgressBar
            android:id="@+id/books_progress"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:elevation="@dimen/fab_elevation"
            android:indeterminateTint="?android:attr/colorPrimary"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>
