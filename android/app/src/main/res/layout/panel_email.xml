<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp">

        <ImageView
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/logo_circle"
            android:contentDescription="@string/app_name"
            android:padding="16dp"
            android:src="@drawable/ic_launcher" />

        <!-- Headline-sized, so the page has a hierarchy of its own
             instead of echoing an app bar title. -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/welcome"
            android:textColor="?android:attr/textColorPrimary"
            android:textSize="28sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:text="@string/welcome_value_1"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="18sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/welcome_value_2"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="16sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:text="@string/welcome_description"
            android:textSize="14sp" />

    </LinearLayout>

    <!-- The field floats at the bottom above the keyboard
         (adjustResize); the shared FAB is the continue action, so an
         empty view holds its slot in the row and the disc floats over
         it, leaving the field exactly where it sat with an inline
         button. -->
    <LinearLayout
        android:id="@+id/email_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:baselineAligned="false"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="8dp"
        android:paddingEnd="16dp"
        android:paddingBottom="16dp">

        <EditText
            android:id="@+id/email_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="@string/email_hint"
            android:inputType="textEmailAddress" />

        <View
            android:layout_width="@dimen/fab"
            android:layout_height="@dimen/fab"
            android:layout_marginStart="16dp" />

    </LinearLayout>

</LinearLayout>
