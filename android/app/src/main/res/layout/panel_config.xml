<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="24dp"
        android:paddingTop="24dp"
        android:paddingEnd="24dp"
        android:text="@string/config_title"
        android:textAppearance="@android:style/TextAppearance.DeviceDefault.Widget.ActionBar.Title"
        android:textColor="?android:attr/textColorPrimary" />

    <TextView
        android:id="@+id/config_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:textColor="?android:attr/textColorSecondary" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <RadioGroup
            android:id="@+id/config_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingEnd="24dp" />

    </ScrollView>

    <!-- Disabled until an option is picked; while connecting the
         loader sits on the FAB (disabled again, its arrow hidden). -->
    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_margin="@dimen/fab_margin">

        <ImageButton
            android:id="@+id/config_continue"
            android:layout_width="@dimen/fab"
            android:layout_height="@dimen/fab"
            android:background="@drawable/fab_background"
            android:contentDescription="@string/email_submit"
            android:elevation="@dimen/fab_elevation"
            android:enabled="false"
            android:src="@drawable/ic_arrow_forward" />

        <ProgressBar
            android:id="@+id/config_progress"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:elevation="@dimen/fab_elevation"
            android:indeterminateTint="?android:attr/colorPrimary"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>
