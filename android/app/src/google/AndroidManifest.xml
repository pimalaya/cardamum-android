<?xml version="1.0" encoding="utf-8"?>
<!-- Google build only: the dismissable support panel, launched over
     MainActivity at most once per rolling 3 days until any tier was
     paid (docs/monetization.md). Never blocks the app. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".billing.SupportActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />
    </application>

</manifest>
